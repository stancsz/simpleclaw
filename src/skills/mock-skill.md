# Mock Skill

This is a mock skill designed for testing the end-to-end swarm execution workflow.
It provides a predictable output for validation.

## Tools
### mock_data
Retrieves static mock data.

**Input Format**:
```json
{
  "query": "string (optional)"
}
```

**Output**:
Returns a JSON string containing mock data.